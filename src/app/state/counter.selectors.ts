// import { createSelector } from '@ngrx/store';
// import { ProductCounterId } from '../interface/app.interface';

// const stateProductCounter = (state: ProductCounterId) => state;

// export const selectProductCounterId = createSelector(
//     stateProductCounter,
//   (state) => state
// );
